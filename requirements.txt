# Core scientific stack
numpy>=1.26,<2.0
scipy>=1.11
pandas>=2.1
scikit-learn>=1.4

# Geospatial
rasterio>=1.3        # GDEM raster I/O
shapely>=2.0         # geometric operations
pyproj>=3.6          # coordinate transformations
fiona>=1.9           # vector I/O (fallback)
geopandas>=0.14      # convenience wrapper around shapely + fiona

# OpenStreetMap data fetching
overpy>=0.7          # Overpass API client
osmium>=3.6          # PBF parsing (optional, faster)

# Path planning
networkx>=3.2        # A* on the adjacency graph

# Plotting
matplotlib>=3.8
seaborn>=0.13        # heatmap (Fig. 3 panel b)

# Statistics
statsmodels>=0.14    # additional Wilcoxon / Shapiro-Wilk options

# Notebooks (optional)
jupyterlab>=4.0
ipykernel>=6.27

# Testing
pytest>=7.4
pytest-cov>=4.1

# Reproducibility utilities
tqdm>=4.66           # progress bars
pyyaml>=6.0          # config files
